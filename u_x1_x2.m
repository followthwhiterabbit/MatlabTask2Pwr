function u = u_x1_x2(t)
%U_X1_X2 a function which generates an input signal that moves the system
% xdot = Ax +  Bu from the state x1 to the state x2 during a time interval
% of the length tk. This function uses the global variables A, B, tk, x1,
% x2. 

%Suggestion. The input signal moving the system from the state x1 to the
%state x2 within the time interval of the length tk, is described by the
%following formulas: 








end 
